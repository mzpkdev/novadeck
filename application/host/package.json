{
  "name": "@novadeck/host",
  "version": "0.0.0",
  "private": true,
  "description": "The Electron wrapper that runs the NovaDeck UI and backend.",
  "author": "mzpkdev",
  "type": "module",
  "main": "./out/main/index.js",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names ui,host \"pnpm --filter @novadeck/ui dev\" \"electron-vite dev\"",
    "build": "electron-vite build",
    "package": "pnpm --workspace-root exec turbo run build --filter=@novadeck/host && electron-builder --dir --publish never",
    "package:linux": "pnpm --workspace-root exec turbo run build --filter=@novadeck/host && electron-builder --linux --x64 --publish never",
    "package:mac": "pnpm --workspace-root exec turbo run build --filter=@novadeck/host && electron-builder --mac --universal --publish never",
    "package:win": "pnpm --workspace-root exec turbo run build --filter=@novadeck/host && electron-builder --win --x64 --publish never",
    "test": "pnpm run build && vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@novadeck/runtime": "workspace:*",
    "@novadeck/ui": "workspace:*",
    "@types/node": "^26.6.2",
    "concurrently": "^10.0.5",
    "electron": "^44.4.3",
    "electron-builder": "^26.15.3",
    "electron-vite": "^5.0.0",
    "typescript": "^7.0.2",
    "vite": "^7.3.6",
    "vitest": "^5.0.1"
  },
  "desktopName": "dev.mzpk.novadeck",
  "productName": "NovaDeck"
}
